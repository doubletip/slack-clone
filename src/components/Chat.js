import React from 'react'
import styled from 'styled-components'

function Chat() {
	return (
		<Container>
			<Header>
			<MessageContainer>

			</MessageContainer>
			<ChantInput></ChantInput>
			</Header>
		</Container>

	)
}

export default Chat

const Container = styled.div``;
const Header = styled.div``;
const MessageContainer = styled.div``;
const ChantInput = styled.div``;
